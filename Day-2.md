---
---

--- Before Day-2 ---
I already knew basic terminal commands and had some experience using Python, Git, and VS Code. I was comfortable navigating directories but did not have a strong understanding of environment variables, shell configuration files, or command chaining.
---

## Day-2 Checklist

- [x] I understand what `PATH` is and why commands like `python` work without full paths
- [x] I can navigate the filesystem without clicking — using `cd`, `ls`, and `pwd` only
- [x] I can read, search, and inspect files using `cat`, `head`, `tail`, `grep`, and `wc`
- [x] I can edit a file using `nano` (open, edit, save, exit)
- [x] I understand pipes (`|`) and redirection (`>`, `>>`, `2>`) and can chain commands
- [x] I can set an environment variable in `.bashrc` and apply it with `source ~/.bashrc`
- [x] I know the difference between `export VAR=value` (available to child processes) and just `VAR=value` (shell-local)

--- After Day-2 ---
I learned how the PATH variable helps the shell locate commands, how to efficiently inspect and search files using command-line tools, and how pipes can connect multiple commands together. I also learned how environment variables work and how shell configuration files like `.bashrc` can be used to customize the development environment.
---

--- Feedback (Suggestions for the TDS Team) ---
The session was easy to follow and the practical exercises helped reinforce the concepts well. The examples were useful for understanding real Linux workflows. It would be great to include a few more challenge exercises at the end for additional practice.
---

---
---

Personal Notes:
The most useful topics today were pipes, file inspection commands, and environment variables. They made the Linux terminal feel much more powerful and efficient for daily development work.